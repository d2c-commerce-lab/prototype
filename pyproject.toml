[tool.ruff]
line-length = 100
target-version = "py312"
exclude = [
  ".git",
  ".venv",
  "venv",
  "node_modules",
  "dist",
  "build",
  "apps/web/node_modules",
  "apps/web/dist",
]

[tool.ruff.lint]
select = ["E", "F", "I", "B"]
ignore = []